# 工程使用、学习与编写思路

## 1. 先明确这个工程是什么

本工程是浙大协同定位算法库及其独立在线演示环境。当前默认闭环为：

```text
上海交大统一时间轴下的各节点瞬时IMU
                    │
                    ▼
每节点15维名义惯性状态传播
                    │
平台间直接测距 ────┼──→ 15N联合误差状态更新
                    │
                    ▼
观测质量状态机 → 动态协同拓扑 → 主参考相对定位
                    │
                    ▼
Localization / Observation / Network → GCS或ROS 2适配层
```

算法库不负责传感器驱动、上海交大时间同步、无线转发/路由、上海交大GCS实现或车辆控制。`ZJCL/UDP`、Python模拟器和浏览器面板用于硬件到位前的独立联调，正式AIBrainBox部署应由上海交大ROS 2节点调用C ABI动态库。

### 1.1 配套架构图和函数级图

建议先按以下顺序看图，再对照后文和源码：

1. [工程总体数据流](images/01_工程总体数据流.png)：先分清上海交大ROS 2、浙大动态库、演示UDP和GCS边界；
2. [源码结构与算法流程](images/02_源码结构与算法流程.png)：把目录和算法阶段对应起来；
3. [在线进程函数级运行流程](images/04_在线进程函数级运行流程.png)：追踪`main()`、UDP接收、解码、C ABI和输出；
4. [IMU与测距协同定位函数调用链](images/05_IMU测距协同定位函数调用链.png)：学习15维单节点传播、15N联合协方差和测距更新；
5. [原始图像点云C ABI函数调用](images/03_原始图像点云C_ABI函数调用.png)：理解新增接口为什么只校验、不进入算法。

图片用于建立整体认识，精确字段和实际行为仍以对应头文件、实现和测试为准。

## 2. 最快验证方法

### 2.1 Windows 10/11开发机

环境：64位Windows、Visual Studio 2022“使用C++的桌面开发”、CMake 3.16以上、Python 3。VS Code建议安装Microsoft C/C++和CMake Tools扩展。

用VS Code打开工程根目录后，第一次选择：

```text
CMake: Select a Kit
→ Visual Studio Community 2022 Release - amd64
```

工程内`.vscode/settings.json`会让CMake Tools在打开时自动配置到`build-vscode`。也可以在Developer PowerShell中执行：

```powershell
cmake -S . -B build-vscode -G "Visual Studio 17 2022" -A x64 -DBUILD_TESTING=ON
cmake --build build-vscode --config Release
ctest --test-dir build-vscode -C Release --output-on-failure
```

如果普通PowerShell找不到`cmake`，可以从“Developer PowerShell for VS 2022”运行，或直接使用VS Code的`CMake: Configure`、`CMake: Build`和`CMake: Run Tests`。

最小自检：

```powershell
.\build-vscode\Release\zju_coop_self_check.exe
```

看到`SELF_CHECK PASS`且进程退出码为0，说明动态库、C ABI、三车静止IMU传播、三边测距、主参考输出、重复包、超时和恢复的软件闭环通过。该结果不代表真实传感器精度、时间同步和盒端性能已经验收。

### 2.2 Ubuntu 22.04与RK3588

```bash
sudo apt update
sudo apt install -y build-essential cmake python3
cmake -S . -B build-arm64 -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON
cmake --build build-arm64 -j"$(nproc)"
ctest --test-dir build-arm64 --output-on-failure
./build-arm64/zju_coop_self_check
cmake --install build-arm64 --prefix install-arm64
```

Windows生成的`.dll/.lib/.exe`不能复制到RK3588运行；RK3588必须重新生成AArch64的`libzju_coop.so`。`.vscode`不是运行依赖，`build-vscode`也不得从Windows复制到Linux继续使用。

## 3. 没有真实数据时的完整在线演示

### 3.1 Windows 10/11 PowerShell

本小节全部命令都在**工程根目录下的PowerShell终端**执行。命令中的反引号是PowerShell续行符，反斜杠是Windows路径分隔符，`.exe`来自Visual Studio多配置构建；不要把这些写法直接复制到Ubuntu Bash。Windows Visual Studio构建的可执行程序位于`build-vscode/Release`。本机Python 3.11安装在`D:\python311`且由`py`启动器发现，但`python`命令未加入`PATH`，所以下文使用`py -3.11`；其他计算机若`python --version`可用，可以把`py -3.11`替换为`python`。依次打开三个PowerShell终端：

```powershell
# 终端1：演示GCS
py -3.11 -B tools\gcs_dashboard.py --udp-port 39002 --http-port 8080

# 终端2：在线算法
.\build-vscode\Release\zju_coop_online.exe --config config\demo.ini

# 终端3：三车100 Hz静止IMU和20 Hz三边测距
py -3.11 -B tools\imu_uwb_simulator.py --host 127.0.0.1 --port 39001
```

浏览器打开`http://127.0.0.1:8080/`。正常情况下应看到节点1为主参考、三个节点定位、三条活动边和网络状态。停止模拟器超过`edge_timeout_ns`后，活动边减少并产生网络告警；重新启动模拟器后应看到恢复过程。

一条命令完成进程级验证：

```powershell
py -3.11 -B tools\imu_online_smoke_test.py `
  --online-exe build-vscode\Release\zju_coop_online.exe `
  --replay-exe build-vscode\Release\zju_coop_replay.exe `
  --config config\demo.ini
```

仅测距兼容模式只用于缺少IMU时回退或历史回归：

```powershell
.\build-vscode\Release\zju_coop_online.exe --config config\range_only_demo.ini
py -3.11 -B tools\uwb_simulator.py --host 127.0.0.1 --port 39001 --rate-hz 20
```

### 3.2 Ubuntu 22.04 Bash

本小节全部命令都在**工程根目录下的Bash终端**执行。假设已经按第2.2节构建到`build-arm64`；Linux单配置生成器的可执行程序直接位于该目录，不带`Release`子目录，也没有`.exe`后缀。依次打开三个Bash终端：

```bash
# 终端1：演示GCS
python3 -B tools/gcs_dashboard.py --udp-port 39002 --http-port 8080

# 终端2：在线算法
./build-arm64/zju_coop_online --config config/demo.ini

# 终端3：三车100 Hz静止IMU和20 Hz三边测距
python3 -B tools/imu_uwb_simulator.py --host 127.0.0.1 --port 39001
```

浏览器同样打开`http://127.0.0.1:8080/`。Ubuntu上的一条命令进程级验证为：

```bash
python3 -B tools/imu_online_smoke_test.py \
  --online-exe build-arm64/zju_coop_online \
  --replay-exe build-arm64/zju_coop_replay \
  --config config/demo.ini
```

若是在RK3588盒端直接运行但没有桌面浏览器，可在同一网络内的开发机访问盒端HTTP地址；同时应根据网络边界显式设置GCS监听地址，不能把`127.0.0.1`误解为跨设备地址。

以上Windows和Ubuntu命令只验证“模拟数据→算法→临时UDP遥测→演示GCS→日志/回放”的软件闭环。它们不能替代AIBrainBox上的真实IMU/UWB驱动、ROS 2消息与QoS、统一时间轴、ARM64负载、长稳和实车精度验收。

## 4. 分阶段学习计划

不要从最大的实现文件开始逐行阅读。按照“先运行建立直觉→边界和契约→数据语义→单车算法→多车融合→质量拓扑→适配回放→ROS 2部署”学习。阶段已经足够多，因此从本版起不再在阶段内部设置“子阶段”或“第几项实践任务”；每个阶段只有一组平级的阅读、实践和回答要求。只有上一阶段能用自己的话解释，才进入下一阶段。

### 阶段0：运行、自检和测试分层

**先看程序：**

1. `src/apps/self_check_main.cpp`：独立自检的`main`，重点看它只调用`run_self_check()`并用退出码表示成功/失败。
2. `src/self_check/self_check.cpp`：从`run_self_check()`开始看，按“创建会话→配置15维惯导→两批静止IMU→三条3/4/5 m测距→step→重复包→超时→恢复”记录每个检查。
3. `tools/imu_online_smoke_test.py`：从`run()`开始看五个阶段，重点区分Python只负责启动进程、发送模拟数据和验证证据，真正的算法运行在C++可执行程序中。
4. `CMakeLists.txt`第`if(BUILD_TESTING)`段：看7项CTest分别启动哪个程序。

**运行顺序：**

```powershell
cmake --build build-vscode --config Release --parallel
ctest --test-dir build-vscode -C Release --output-on-failure
.\build-vscode\Release\zju_coop_self_check.exe
py -3.11 -B tools\imu_online_smoke_test.py `
  --online-exe build-vscode\Release\zju_coop_online.exe `
  --replay-exe build-vscode\Release\zju_coop_replay.exe `
  --config config\demo.ini
```

**实践记录：**保存编译器/CMake/Python版本、CTest七项名称、self-check末行、在线smoke末行；用一句话分别说明编译、单元测试、自检、进程级smoke和实车验收的边界。

**通过标准：**能够解释`SELF_CHECK PASS`为什么不等于ROS 2、RK3588和实车精度已经验收。

### 阶段1：系统边界和完整数据流

1. 阅读`README.md`的“已实现范围”“交付边界与分工”“工程结构”，确认三方职责。
2. 阅读`docs/01_算法与可观测性说明.md`，确认当前主输出是主参考节点坐标系内的相对位置，不是地图绝对位置。
3. 阅读`docs/04_AIBrainBox部署与上交ROS2适配说明.md`第2、4、5、6、9节，确认ROS 2消息、C ABI和GCS结果的映射边界。
4. 阅读`src/apps/online_main.cpp`的`run()`、`tools/imu_uwb_simulator.py`和`tools/gcs_dashboard.py`，确认Windows演示链路中的发送、接收、算法调用和展示进程。
5. 正式AIBrainBox链路答案：
   `传感器 → 上海交大驱动/字段解析/统一时间轴 → ROS 2标准或自定义消息 → 上海交大wrapper逐字段映射为C ABI结构 → 浙大算法库libzju_coop.so → C ABI定位/观测/网络输出 → 上海交大ROS 2结果发布与GCS网关 → GCS`。
6. 当前Windows演示链路答案：
   `Python模拟器 → ZJCL输入帧/UDP 39001 → zju_coop_online.exe（解码后调用同一算法库）→ ZJCL输出帧/UDP 39002 → gcs_dashboard.py → 浏览器GCS看板`。
7. 阶段1只做一次实践：不看本段答案，独立重画上述两条链，并在每个箭头上标明数据形式。
8. 阶段1完成要求：能说明ZJCL/UDP只是无ROS硬件时的演示适配层；正式盒端由ROS 2 wrapper直接调用C ABI。时间同步、无线转发/路由和车辆控制分别属于上海交大平台层、通信层和规划控制层，不属于浙大协同定位算法核心。

### 阶段2：C ABI生命周期和公开数据结构

**阅读顺序：**

1. `examples/sdk_consumer.cpp`：从`main()`按阶段1至阶段5阅读，记录所有公开函数的调用顺序。
2. `include/zju_coop/c_api.h`：先看版本常量、`zju_coop_handle_t`和`*_init`，再看`create/configure/push/step/destroy`。
3. `tests/c_header_smoke.c`：看纯C编译单元如何验证公开头不依赖C++。
4. `tests/test_c_api.cpp`：优先看版本/尺寸错误、深拷贝、两阶段step、stride、失败不部分写入和同一handle串行调用相关用例。

**重点问题：**`struct_size`、`abi_version`、`stride`、不透明handle、调用方内存所有权、为什么C结构不是网络序列化格式。

**实践任务：**运行`zju_coop_sdk_consumer.exe`，将控制台的三个节点输出与`examples/sdk_consumer.cpp`中3/4/5 m输入逐项对应。

**通过标准：**不看代码写出完整调用顺序，并能解释为什么ROS 2消息必须逐字段映射。

### 阶段3：时间戳、节点、坐标系和配置

**阅读顺序：**

1. `include/zju_coop/types.hpp`中的`ImuPacket`、`RangePacket`和输出结构。
2. `config/demo.ini`的`[engine]`、`[filter]`、`[degradation]`、`[inertial]`和三个`[node.*]`。
3. `src/config/ini_config.hpp/.cpp`：重点看白名单、数值范围、跨字段校验和错误行号。
4. `tests/test_ini_config.cpp`：按正常配置、未知键、非法数值、缺失数据、资源上限阅读。

**重点问题：**`timestamp_ns`与`receive_timestamp_ns`的区别、ENU/FLU、`frame_id`不是坐标变换、序号和节点号的作用、为何参数不硬编码。

**实践任务：**制作一张“ROS 2字段→C ABI字段→单位→坐标系→无效处理”的IMU/UWB映射表。

**通过标准：**能说明时间同步失效时为什么必须报告无效而不能伪造时间戳。

### 阶段4：单节点15维IMU递推

**阅读顺序：**

1. `src/core/quaternion.hpp/.cpp`：看`Quaternion::exp`、乘法、旋转和归一化。
2. `tests/test_quaternion.cpp`：先用测试理解SO(3)指数映射的预期行为。
3. `src/core/inertial_eskf15.hpp`：先掌握名义状态、15维误差顺序和`ImuProcessingResult`。
4. `tests/test_inertial_eskf15.cpp`：依次看首帧、静止、恒加速度、恒角速度、零偏、乱序、首帧姿态和消息协方差。
5. `src/core/inertial_eskf15.cpp`的`push_imu()`：按阶段1至阶段7追踪校验、中值输入、子步、姿态/速度/位置、`Phi/Qd`和事务回滚。

**实践任务：**在纸上推导静止FLU输入`(0,0,+g)`为何在ENU中得到零加速度；说明`Quaternion::exp(ωΔt)`和`Phi≈I+FΔt`是两种不同的“指数/离散化”概念。

**通过标准：**能解释当前是瞬时量递推ESKF，不是调用方预积分，也不是完整李群不变EKF。

### 阶段5：多节点15N协同测距更新

**阅读顺序：**

1. `src/core/cooperative_inertial_ekf.hpp`：看节点块布局、15N联合协方差和公开更新接口。
2. `tests/test_cooperative_inertial_ekf.cpp`：重点看单节点传播只更新对应块、测距耦合节点、NIS拒绝、主参考相对协方差。
3. `src/core/cooperative_inertial_ekf.cpp`：定位距离雅可比、创新、完整增益、Joseph更新和误差注入。
4. `src/core/range_ekf.*`与`tests/test_range_ekf.cpp`：只作为无IMU回退模型对照，不要与默认15维路径混淆。

**纸面公式：**

```text
h(x)=||p_j-p_i||
H_i=-(p_j-p_i)^T/||p_j-p_i||
H_j=+(p_j-p_i)^T/||p_j-p_i||
NIS=innovation²/innovation_variance
```

**通过标准：**能解释为什么一条两车测距可能通过交叉协方差影响其他相关节点，以及为什么相对协方差不是简单相加两个节点方差。

### 阶段6：NLOS、NIS、退化状态机和动态拓扑

**阅读顺序：**

1. `src/core/degradation_monitor.hpp/.cpp`：重点看滑窗计数、reason mask、状态持续时间和融合动作。
2. `tests/test_degradation.cpp`：看30% NLOS边界、有效率、实际频率、残差拒绝、输入溢出和恢复。
3. `src/core/rigidity.hpp/.cpp`及`tests/test_rigidity.cpp`：看连通、可达、非共线三角形和秩判断。
4. `src/core/engine.cpp`的`push_range()`和`step()`：看质量动作如何决定是否更新滤波、哪些边进入活动拓扑。
5. `tests/test_engine.cpp`：重点看NLOS降权、NIS原因、边超时、动态非固定边、时间偏差和事务失败。

**实践任务：**分别预测“高NLOS、低频率、停止输入、三车共线、缺一条边”时Observation和Network会如何变化，再用测试核对。

**通过标准：**能区分设备NLOS字段、算法NIS、滑窗有效率、活动边和二维几何可观性。

### 阶段7：临时协议、UDP、ZJLG和回放

**阅读顺序：**

1. `docs/03_临时在线协议说明.md`：先看ZJCL头、消息类型、CRC覆盖范围和ZJLG记录。
2. `src/protocol/wire_protocol.hpp/.cpp`：重点看`encode_frame()`、`decode_frame()`及先CRC后载荷语义校验。
3. `tools/zjcl_protocol.py`和`tools/test_zjcl_protocol.py`：看Python/C++黄金字节如何锁定同一布局。
4. `src/net/udp_socket.*`和`tests/test_udp_socket.cpp`：看回环收发、超时和资源移动。
5. `src/protocol/event_log.*`、`tests/test_event_log.cpp`和`src/apps/replay_main.cpp`：看日志如何保存完整帧、回放为何跳过历史Output并重新计算Input。

**实践任务：**指出`ZJCL`、`ZJLG`、CRC、UDP和C ABI分别处于哪一层；手工修改日志或帧一个字节，观察严格校验如何失败。

**通过标准：**能解释正式ROS 2盒端为什么不需要再经过当前ZJCL/UDP入口。

### 阶段8：在线编排、Python工具和部署

**阅读顺序：**

1. `src/apps/app_support.hpp/.cpp`：看`AlgorithmSession`如何管理C ABI句柄，`TelemetryEncoder`如何生成状态和告警。
2. `src/apps/online_main.cpp`：完整跟踪UDP输入、日志、周期step和输出。
3. `tools/imu_uwb_simulator.py`与`tools/uwb_simulator.py`：看静止IMU、3/4/5 m测距、噪声、NLOS和丢包如何生成。
4. `tools/gcs_dashboard.py`与`tools/test_gcs_dashboard.py`：看GCS只展示结果，不重新计算协同定位。
5. `tools/online_smoke_test.py`与`tools/imu_online_smoke_test.py`：看Python如何启动真实C++进程并收集可判定证据。
6. `docs/04_AIBrainBox部署与上交ROS2适配说明.md`：最后学习Ubuntu 22.04、ARM64、ROS 2 wrapper和实机验收边界。

**实践任务：**先在Ubuntu 22.04构建/测试同一源码，再实现或评审一个参考ROS 2 wrapper的逐字段映射；Gazebo仅在需要车辆运动、TF、相机/点云或规划控制闭环时加入，不作为当前库验收前置条件。

**通过标准：**能列出Windows测试通过后仍需完成的Ubuntu、ROS 2、ARM64、真实时间同步、真实传感器、长稳和精度验证。

## 5. 编写思路

### 5.1 核心与框架解耦

核心只接受普通C/C++结构。上海交大wrapper负责把ROS 2消息字段、坐标轴和时间戳转换后调用库，因而算法可以在Windows无ROS环境测试，也能在Ubuntu/RK3588编译为`.so`。

### 5.2 先冻结数据语义再写算法

每个输入必须先明确节点号、序号、测量时间、接收时间、单位、坐标系、有效位和质量字段。缺少统一时间戳的距离值不能直接进入协同滤波；仅改变`frame_id`字符串也不能代替真实轴系变换。

### 5.3 名义状态与误差状态分离

每节点维护位置、速度、姿态和两类零偏名义状态；15维小误差用于协方差传播与量测更新。IMU高频传播对应节点状态块和交叉协方差，低频平台间测距在同一联合状态上异步更新。

### 5.4 失败必须可诊断且不污染状态

乱序、重复、时间超限、NLOS、NIS拒绝和数值失败分别返回不同结果。矩阵或误差注入先在候选副本完成，只有全部检查成功才提交，避免在线状态出现难以复现的半更新。

### 5.5 质量、拓扑和滤波分层

质量监测决定一条观测正常、降权、暂缓还是剔除；滤波器只执行数值更新；拓扑模块依据当前有效边计算可达与可观状态。三者分开后，后续视觉/激光观测可以复用质量和拓扑框架，不必重写通信或GCS。

### 5.6 配置代替源码调参

运行阈值进入INI并接受严格解析。增加新参数时必须同时修改配置模型、白名单、范围/组合校验、默认配置、C ABI映射、测试和文档，不能只在`.cpp`中增加隐藏常量。

## 6. 后续增加激光雷达和视觉的正确顺序

1. 先与上海交大冻结原始`PointCloud2`/图像、相机内外参、时间同步状态和算力边界；
2. 在浙大前端模块中从原始数据提取`from_node/to_node/timestamp/measurement/covariance`及专用质量字段；
3. 为核心增加普通C++相对位姿观测结构，不把ROS消息写入核心；
4. 扩展质量评价和联合滤波量测模型；
5. 通过新增C ABI结构或升级ABI版本暴露接口，不能修改v1结构中既有字段顺序；
6. 上海交大wrapper再映射ROS 2输入，上海交大GCS只接收定位、拓扑、状态和告警结果。

当前没有真实激光/视觉前端资料时，不应伪造已经实现这些算法；已有扩展边界只保证未来可以按上述顺序接入。

## 7. 修改后的最低验证要求

每次改动至少执行：

```text
1. Release重新编译
2. zju_coop_self_check
3. ctest --output-on-failure
4. 与修改模块对应的异常/边界测试
5. 若改协议，同时运行C++和Python黄金字节测试
6. 若改在线链路，同时运行IMU+测距进程级冒烟测试
```

若修改C ABI、坐标系、时间语义、状态维数或协议布局，必须在提交说明中单独列出兼容性影响，不能只写“优化代码”。

## 8. 常见误区

- `namespace zju::coop`是C++17合法语法；VS Code报错时先检查IntelliSense工具链，不要改回旧语法掩盖配置问题。
- 标准`sensor_msgs/Imu`没有温度；温度属于模组私有状态，不应向标准消息或当前算法输入虚构字段。
- 三车三边测距不能给出全球绝对位置；当前初值和主参考只固定相对坐标自由度。
- `yaw_valid=false`、`z_valid=false`必须由GCS尊重，不能因内部状态是三维就显示为有效航向/高度。
- Windows测试通过不能替代RK3588实测，实机仍须重新记录编译、ABI、负载、时钟和长时间运行结果。
