# 18 分布式改造实施计划与验收记录

## 1. 实施结论

2026-08-17 已按“最简 IMU＋UWB 到 GCS”目标完成源码实施，并把对上交的 `NodeState v1` 冻结为 `Header/node_id/p/v/q/valid`：

```text
上交每盒 sensor_msgs/msg/Imu publisher
  -> 浙大 zju_local_inertial_node
  -> 单车 15 维惯导＋本机 P15
  -> 紧凑 NodeState
  -> ROS 2 publish/subscribe + DDS + 5G Mesh IP
  -> 参考车 zju_cooperative_fusion_node
  <- 上交 /uwb/range
  -> 二维 UWB 修正
  -> /cooperative_localization/poses_2d @ 10 Hz
  -> 上交 GCS
```

跨车不发原始 IMU，也不发 `bg/ba` 或 225 个协方差元素。当前仍由参考车集中运行一个很小的 `2(N-1)` 维修正器，三车时只是 4 维；它不是原有的 45 维原始 IMU 联合滤波。若现场连紧凑 NodeState 都不能汇聚到任一节点，需另立项设计无中心信息滤波和交叉相关性管理，本版本没有实现该能力。

## 2. 本轮精确文件清单

### 2.1 纯 C/C++ 核心和 C ABI

| 文件 | 本轮职责 |
|---|---|
| `CMakeLists.txt` | 把 `distributed_fusion2d.cpp` 和其测试加入既有目标；仍只注册 7 项 CTest |
| `src/core/distributed_fusion2d.hpp` | 定义普通 C++ `NodeState`、配置、二维修正器和输出类型；不包含 ROS 头 |
| `src/core/distributed_fusion2d.cpp` | 实现 64 帧有界缓存、严格时序、插值、100 ms 有限外推、UWB 二维修正、NIS 门限、持久后验和 Pose2D 输出 |
| `include/zju_coop/c_api.h` | 增量声明 `zju_coop_node_state_t`、状态导出函数及独立 distributed handle 函数；保留旧 v1/Pose2D v2 布局 |
| `src/api/c_api.cpp` | 实现 C 结构校验、异常边界、单节点惯导状态导出和独立分布式句柄调度 |
| `tests/test_distributed_fusion2d.cpp` | 覆盖相对 ENU/yaw、UWB 非零修正、后验保留、对齐、超时、非法距离、INVALID 质量码、每边去重和重建句柄清空会话状态 |
| `tests/test_c_api.cpp` | 覆盖单节点惯导导出 NodeState 及 NodeState＋UWB＋Pose2D C ABI 端到端 |
| `tests/c_header_smoke.c` | 用纯 C 编译器锁定新结构大小/偏移与公开函数声明 |

以下集中式基线文件没有被删除或替换：

```text
src/core/inertial_eskf15.hpp/.cpp
src/core/cooperative_inertial_ekf.hpp/.cpp
src/core/range_ekf.hpp/.cpp
src/core/engine.hpp/.cpp
tests/test_inertial_eskf15.cpp
tests/test_cooperative_inertial_ekf.cpp
tests/test_range_ekf.cpp
```

### 2.2 ROS 2 消息和节点

| 文件 | 职责 |
|---|---|
| `ros2/cooperative_localization_msgs/package.xml` | 浙大正式定位消息包依赖 |
| `ros2/cooperative_localization_msgs/CMakeLists.txt` | 生成 3 种 ROS 2 消息类型支持 |
| `ros2/cooperative_localization_msgs/msg/NodeState.msg` | 已冻结的跨车紧凑 `header/node_id/p/v/q/valid`，不含 sequence、初始化代次、零偏和协方差 |
| `ros2/cooperative_localization_msgs/msg/VehiclePose2D.msg` | GCS 单车二维位置、ENU 航向和有效位 |
| `ros2/cooperative_localization_msgs/msg/CooperativePose2DArray.msg` | GCS 同一算法时刻的全部车辆数组 |
| `ros2/zju_coop_test_msgs/package.xml` | 临时 UWB 消息包依赖 |
| `ros2/zju_coop_test_msgs/CMakeLists.txt` | 生成临时 UWB 类型支持 |
| `ros2/zju_coop_test_msgs/msg/UwbRange.msg` | 精确复制已知 `Header/src_id/target_id/distance` 字段 |
| `ros2/zju_coop_ros2/package.xml` | C++ 节点、Python 仿真器、消息和 launch_testing 依赖 |
| `ros2/zju_coop_ros2/CMakeLists.txt` | 只从 `ZJU_COOP_SDK_ROOT` 查找本机 SDK，编译两节点、安装仿真器并注册三项 launch 测试 |
| `ros2/zju_coop_ros2/src/local_inertial_node.cpp` | 订阅本车标准 IMU，调用单节点 C ABI，发布 NodeState，拒绝静默的处理失败 |
| `ros2/zju_coop_ros2/src/cooperative_fusion_node.cpp` | 订阅 NodeState/UWB，调用独立二维修正句柄，10 Hz 发布 GCS 数组，记录未接受的 UWB 诊断 |
| `ros2/zju_coop_ros2/scripts/three_vehicle_simulator.py` | 以解析运动学生成三车 100 Hz IMU、20 Hz UWB 和真值；支持固定 seed 白噪声、周期掉测和正距离离群值 |
| `ros2/zju_coop_ros2/test/test_pipeline.launch.py` | 启动三个 local＋一个 fusion；断言 5 m 基线被 4 m UWB 向正确方向修正，并校验三车 yaw/GCS 字段 |
| `ros2/zju_coop_ros2/test/test_required_initialization.launch.py` | 分别缺少初始 `p/v/q` 时断言 local 节点以退出码 1 失败 |
| `ros2/zju_coop_ros2/test/test_dynamic_pipeline.launch.py` | 以动态三车轨迹验证 100 Hz IMU、20 Hz 延迟 UWB、20 Hz NodeState、10 Hz GCS、测距时刻对齐和 UWB 修正不回馈本地 INS |

### 2.3 Bringup

| 文件 | 职责 |
|---|---|
| `ros2/zju_coop_bringup/package.xml` | launch 包运行依赖 |
| `ros2/zju_coop_bringup/CMakeLists.txt` | 安装 `config/` 和 `launch/`，排除 Python 缓存，注册一键仿真验收 |
| `ros2/zju_coop_bringup/config/vehicle_1.yaml` | 1 号车公共 ENU 初值、FLU 姿态、零偏和发布频率 |
| `ros2/zju_coop_bringup/config/vehicle_2.yaml` | 2 号车同类参数 |
| `ros2/zju_coop_bringup/config/vehicle_3.yaml` | 3 号车同类参数 |
| `ros2/zju_coop_bringup/config/fusion.yaml` | node IDs、参考车、测距标准差、超时和 10 Hz 输出 |
| `ros2/zju_coop_bringup/launch/vehicle.launch.py` | 每盒启动 local，仅参考车用 `run_fusion=true` 增启 fusion，完成 Topic remap |
| `ros2/zju_coop_bringup/launch/simulation.launch.py` | 一键启动三个 local、一个 fusion 和仿真器；2 号本地初值故意带 0.5 m 偏差以证明 UWB 修正生效 |
| `ros2/zju_coop_bringup/test/test_simulation_launch.launch.py` | 从安装树加载一键 launch，对比真值与 GCS，覆盖动态运动、白噪声、周期掉测和周期正距离离群值 |

## 3. 先失败测试、再实现、再回归

本轮保持下列顺序，新断言都进入既有 `zju_coop_tests` 目标，不改变 CTest 7 项验收数字：

1. 先记录原工程 7/7 CTest 和 14/14 独立自检基线。
2. 先在 `tests/test_distributed_fusion2d.cpp` 写相对 ENU、UWB 修正持久性、对齐、超时和无向边去重的失败测试，再实现 `DistributedFusion2D`。
3. 先在 `tests/test_c_api.cpp` 和 `tests/c_header_smoke.c` 引用未实现的 NodeState/distributed 入口，再增量实现 C ABI，保留旧结构静态布局断言。
4. 先写 `test_pipeline.launch.py` 的 IMU→NodeState→UWB→Pose 端到端断言，再实现 ROS 2 两节点和 remap。
5. 审计发现原 UWB 测试创新为 0 后，把基线改为 5 m、量测改为 4 m，明确断言输出向 4 m 改善，使“UWB 路径完全失效”时测试必然失败。
6. 先写缺少位置、速度、姿态初值的三进程负测试，再锁定节点必须 fail-fast 的参数规则。
7. 最终审计对 `status=INVALID, valid=true` 新增失败测试；它在修正前得到 `Tests: 191, Failures: 1`，将质量码条件改为拒绝 `status>=2` 后恢复 191/191。
8. 冻结接口前，先把 C/C++ 布局断言改为 120 字节、ROS introspection 断言改为六个业务字段，并删除调用端初始化代次，证明旧实现编译失败、旧生成消息字段测试失败；再删除整条无效会话语义。
9. 新增“重建 distributed handle 清空历史和 UWB 修正”的测试，明确无会话编号时唯一支持的重新初始化方式；最终核心断言数为 192 项。
10. 使用全新 Release 构建树、全新 staging SDK 和全新 colcon 构建树运行全量回归，不使用修改前的可执行文件作通过证据。
11. 先让动态 ROS 测试因缺少仿真 launch 失败，再实现解析三车仿真器和一键启动；最终由独立 truth 消息校验动态轨迹和 UWB 后验。
12. 将三车 IMU 和三条 UWB 边分成六条固定随机流，即使某个测距历元被丢弃也继续推进该边随机流，避免故障开关改变其他传感器的后续样本。

## 4. 纯 C++ 构建与测试命令

从源码根目录执行：

```bash
cmake -S . -B build-node-state-v1-acceptance \
  -DCMAKE_BUILD_TYPE=Release \
  -DBUILD_TESTING=ON \
  -DCMAKE_INSTALL_PREFIX="$PWD/stage/zju_coop-node-state-v1"

cmake --build build-node-state-v1-acceptance --parallel
./build-node-state-v1-acceptance/zju_coop_tests
ctest --test-dir build-node-state-v1-acceptance --output-on-failure
./build-node-state-v1-acceptance/zju_coop_self_check
cmake --install build-node-state-v1-acceptance
```

安装树 C++ consumer：

```bash
c++ -std=c++17 examples/sdk_consumer.cpp \
  -I"$PWD/stage/zju_coop-node-state-v1/include" \
  -L"$PWD/stage/zju_coop-node-state-v1/lib" \
  -Wl,-rpath,"$PWD/stage/zju_coop-node-state-v1/lib" \
  -lzju_coop \
  -o build-node-state-v1-acceptance/sdk_consumer_installed
./build-node-state-v1-acceptance/sdk_consumer_installed
```

安装树纯 C 头文件和链接冒烟：

```bash
cc -std=c11 \
  -I"$PWD/stage/zju_coop-node-state-v1/include" \
  -c tests/c_header_smoke.c \
  -o build-node-state-v1-acceptance/c_header_smoke_installed.o

c++ build-node-state-v1-acceptance/c_header_smoke_installed.o \
  -L"$PWD/stage/zju_coop-node-state-v1/lib" \
  -Wl,-rpath,"$PWD/stage/zju_coop-node-state-v1/lib" \
  -lzju_coop \
  -o build-node-state-v1-acceptance/c_header_smoke_installed
./build-node-state-v1-acceptance/c_header_smoke_installed
```

## 5. ROS 2/colcon 构建与测试命令

ROS 节点必须链接与目标 CPU 匹配的新 SDK；本机验收使用上节 x86-64 staging：

```bash
source /opt/ros/humble/setup.bash

colcon --log-base log/ros2-node-state-v1-build build \
  --base-paths ros2 \
  --build-base build/ros2-node-state-v1 \
  --install-base install/ros2-node-state-v1 \
  --packages-up-to zju_coop_bringup \
  --cmake-args \
    -DCMAKE_BUILD_TYPE=Release \
    -DZJU_COOP_SDK_ROOT="$PWD/stage/zju_coop-node-state-v1"

source install/ros2-node-state-v1/setup.bash

RMW_IMPLEMENTATION=rmw_fastrtps_cpp \
colcon --log-base log/ros2-node-state-v1-test test \
  --base-paths ros2 \
  --build-base build/ros2-node-state-v1 \
  --install-base install/ros2-node-state-v1 \
  --packages-select \
    cooperative_localization_msgs \
    zju_coop_test_msgs \
    zju_coop_ros2 \
    zju_coop_bringup \
  --return-code-on-test-failure

colcon test-result \
  --test-result-base build/ros2-node-state-v1 \
  --verbose
```

接口和动态库审计：

```bash
source /opt/ros/humble/setup.bash
source install/ros2-node-state-v1/setup.bash

ros2 interface show cooperative_localization_msgs/msg/NodeState
ros2 interface show cooperative_localization_msgs/msg/VehiclePose2D
ros2 interface show cooperative_localization_msgs/msg/CooperativePose2DArray
ros2 interface show zju_coop_test_msgs/msg/UwbRange

ldd install/ros2-node-state-v1/zju_coop_ros2/lib/zju_coop_ros2/zju_local_inertial_node
ldd install/ros2-node-state-v1/zju_coop_ros2/lib/zju_coop_ros2/zju_cooperative_fusion_node
```

### 5.1 一键解析仿真

在完成上述 build 并 source 对应 install 树后，直接运行：

```bash
RMW_IMPLEMENTATION=rmw_fastrtps_cpp \
ros2 launch zju_coop_bringup simulation.launch.py
```

默认为无噪声理想链路；可用以下命令做可重复的轻量故障注入：

```bash
RMW_IMPLEMENTATION=rmw_fastrtps_cpp \
ros2 launch zju_coop_bringup simulation.launch.py \
  uwb_noise_std_m:=0.02 \
  uwb_drop_every_n:=10 \
  uwb_nlos_every_n:=25 \
  gyro_noise_std_rad_s:=0.0002 \
  accel_noise_std_m_s2:=0.01
```

GCS 输出为 `/cooperative_localization/poses_2d`，对照真值为 `/simulation/ground_truth/poses_2d`。该仿真器只用解析平面轨迹验证秒级到短时软件链路；周期“NLOS”实际是 2–3 边的正距离离群值，不是真实多径传播、NLOS 识别、车辆动力学、时钟漂移或网络模型。通过该仿真不等于长时漂移、RK3588 性能或实车精度已验收。

### 5.2 可选 Gazebo Fortress 仿真

Gazebo 只安装在 Ubuntu 22.04 x86-64 开发机，不加入 RK3588 运行镜像。Humble 使用官方配套的 Fortress：

```bash
sudo apt-get update
sudo apt-get install ros-humble-ros-gz
```

安装依赖后使用全新目录构建和测试：

```bash
source /opt/ros/humble/setup.bash

colcon --log-base log/ros2-milestone-v0.3.0-build build \
  --base-paths ros2 \
  --build-base build/ros2-milestone-v0.3.0 \
  --install-base install/ros2-milestone-v0.3.0 \
  --packages-up-to zju_coop_gazebo zju_coop_bringup \
  --cmake-args \
    -DCMAKE_BUILD_TYPE=Release \
    -DZJU_COOP_SDK_ROOT="$PWD/stage/zju_coop-milestone-v0.3.0"

source install/ros2-milestone-v0.3.0/setup.bash

PYTHONDONTWRITEBYTECODE=1 ROS_DOMAIN_ID=107 \
RMW_IMPLEMENTATION=rmw_fastrtps_cpp \
colcon --log-base log/ros2-milestone-v0.3.0-test test \
  --base-paths ros2 \
  --build-base build/ros2-milestone-v0.3.0 \
  --install-base install/ros2-milestone-v0.3.0 \
  --packages-select cooperative_localization_msgs zju_coop_test_msgs \
    zju_coop_ros2 zju_coop_bringup zju_coop_gazebo \
  --executor sequential \
  --return-code-on-test-failure

colcon test-result --test-result-base build/ros2-milestone-v0.3.0 --verbose
```

手工启动：

```bash
source /opt/ros/humble/setup.bash
source install/ros2-milestone-v0.3.0/setup.bash

ROS_DOMAIN_ID=107 RMW_IMPLEMENTATION=rmw_fastrtps_cpp \
ros2 launch zju_coop_gazebo three_vehicle_gazebo.launch.py \
  headless:=false
```

该命令默认启动 `ros2 bag record --use-sim-time`，保存 `/clock`、三车 IMU、UWB、truth、NodeState 和 GCS 输出到当前目录的 `bags/gazebo_three_vehicle_<时间戳>/`。临时不录制时追加 `record_bag:=false`；指定位置时追加 `bag_output:=/绝对/路径`。退出 launch 后录包进程先刷新缓存并生成 `metadata.yaml` 与 SQLite 数据文件。

Gazebo 首版先暂停并发布零速度命令，2.5 秒后统一解暂停；仿真时间开始后静止 `1 s`，再由 DiffDrive 以 `0.30 m/s²` 从零速平滑起步。1、3 号车分别以 `0.40/0.25 m/s` 直行；2 号车以 `0.30 m/s` 前进，并在 8 秒内执行 `ω=0.15·sin(2πt/8)` rad/s 的一次平滑 S 弯，随后直行。正常演示的三车 local 初始位姿与 Gazebo 真值一致；仅自动验收给 2 号加入 0.5 m 东向初值误差。运行门禁为：算法相关消息时间戳单调；静止段三车角速度和水平比力近零、竖轴约 `+9.80665 m/s²`；2 号 truth 航向峰值为 `0.30~0.55 rad`、结束后回正到初值 `±0.12 rad`、横向位移超过 `0.25 m`；IMU 持续出现先正后负的 Z 轴角速度，角加速度 P95 不超过模型限幅留量 `0.55 rad/s²`；航向峰值时刻 GCS 与 truth 的误差小于 `0.20 rad`；车辆间距大于 `1.2 m`；三车 NodeState 连续；三条 UWB 边分别具有正有限距离、至少 180 个样本和严格递增时间戳；最终 GCS 三车有效且位置/航向与同历元 truth 误差通过门限；测试中 2 号本地初始误差仍保留，而 GCS 后验误差至少改善 20%。启动文件按 `ROS_DOMAIN_ID` 派生独立 `IGN_PARTITION`，防止其他 Gazebo 实例污染仿真时间和传感器话题。当前 VMware 环境的 GUI 默认使用 OGRE1，避免 OGRE2 模型消失和闪烁。

## 6. 2026-08-17 至 2026-08-18 实际验收结果

| 检查 | 实际结果 | 结论边界 |
|---|---|---|
| Release 全量构建 | GCC 11.4.0，构建成功，ELF 导出审计成功 | Ubuntu 22.04 x86-64 |
| C++ 单元测试 | `Tests: 192, Failures: 0` | 含冻结 NodeState、重建清理、新分布式核心和旧基线 |
| CTest | `7/7` | 现有 7 项数量未变 |
| 独立自检 | `passed=14, failed=0` | 现有 14 项未增删 |
| 安装树消费 | 纯 C 冒烟和外部 C++ consumer 均成功 | 仅使用公开头和 `libzju_coop.so` |
| SDK 架构 | `ELF 64-bit ... x86-64` | 不是 RK3588/AArch64 产物 |
| colcon build | 5 个包全部成功 | ROS 2 Humble x86-64；含消息、节点、bringup 和可选 Gazebo 包 |
| colcon test | 汇总 16 tests，0 error/failure/skip | 5 个 ROS/Gazebo 包顺序回归；显式使用 `rmw_fastrtps_cpp` |
| 生成树一致性 | 当前 staging 公开头、ROS 消息、仿真器和 launch 均与源码一致 | launch 验收从 ament index 加载安装树，不直接引用源码 launch |
| 节点 launch 测试 | 5 个 launch suite 全部通过 | 静态闭环、必填初值、解析动态对齐、bringup 与 Gazebo 物理闭环；同机 Fast DDS |
| UWB 可观测更新 | 断言 2 号车 5 m 初始基线被 4 m 测距向 4 m 拉近 | 证明 ROS UWB 路径真正修改后验 |
| 动态仿真 | 100 Hz IMU、20 Hz 延迟 UWB、20 Hz NodeState、10 Hz GCS；truth 与 GCS 对比通过 | 包含 0.5 m 本地基线误差、白噪声、周期掉测和正距离离群值；只是短时解析模型 |
| 必填初值 | 缺任一 `p/v/q` 的三节点均 FATAL 且 exit 1 | 不伪造零初值有效结果 |
| Gazebo 首版 | Fortress 6.18.0/`ros_gz 0.244.25`；资产与 headless 闭环 `2/2`；自动录包的 8 个 Topic 均有非零消息 | 只验收 x86-64 同机物理/消息/算法/录包链路；未验收 RK3588、网络和实车精度 |

Release 编译仍会报出 GCC 11 对旧 `wire_protocol.cpp` 中 `std::vector::insert` 的 `-Wstringop-overflow` 优化警告。它没有导致本次构建或 192/192、7/7 失败，也不在新 ROS 数据路径上；在将交付构建升级为 `-Werror` 之前，应单独确认这是 GCC 11 误报还是容器容量边界问题。

## 7. 三盒在线运行全流程

1. 上交在每个传感器盒上启动数据节点，发布本车 `sensor_msgs/msg/Imu`；可见 UWB 节点发布 `/uwb/range`。
2. 三台盒子都使用相同 `ROS_DOMAIN_ID`和 `RMW_IMPLEMENTATION=rmw_fastrtps_cpp`；5G Mesh 只需提供三机 IP 可达和 DDS 发现/数据通路。
3. 分别把真实公共 ENU 初始 `p/v/q/bg/ba` 写入本车 YAML，并保证三车使用同一 ENU 原点、轴向和生效时刻。三份演示 YAML 的数字不能直接用于实车。
4. 每个车载盒启动 `zju_local_inertial_node`，通过 launch remap 把内部 `imu` 连到上交本车 IMU topic。
5. 两个从车用 `run_fusion=false`，只发布 `/cooperative_localization/node_state`；参考车用 `run_fusion=true`，同时启动自己的 local 和 fusion。
6. DDS 把三车紧凑 NodeState 传到参考车，fusion 按 `/uwb/range.header.stamp` 对齐两端状态并更新 4 维位置修正量。
7. fusion 以 10 Hz 发布一条 `CooperativePose2DArray`，其 `vehicles` 含三车；GCS 只订阅该 topic，不直接读 NodeState 或原始 IMU。
8. 运行期监看 local 的 IMU disposition 和 fusion 的 UWB processing/update/NIS 节流日志。“节点在运行”不等于“每个输入已被算法消费”。

如果 5G Mesh 不转发组播发现，使用 Fast DDS Discovery Server 或静态 peer 配置解决，不改算法核心，也不把 5G 驱动写进 ROS 节点。

## 8. 三级验收门禁

### 8.1 Ubuntu 22.04 x86-64

全部条件：

- 全新 Release 构建成功；
- `zju_coop_tests` 无失败；
- CTest 持续为 7/7；
- 独立自检持续为 14/14；
- 安装树的纯 C 和外部 C++ consumer 均独立编译、链接、运行；
- 旧集中式 45 维、UWB-only 和新分布式用例同时存在；
- 输出无 NaN/Inf，字段、坐标、时间和 ABI 布局与文档一致。

本轮该门禁已通过。

### 8.2 ROS 2/DDS

分两层记录：

- 同机软件门禁：Humble，`rmw_fastrtps_cpp`，四包 build/test，非零 UWB 修正和必填初值负测试。本轮已通过。
- 跨三盒网络门禁：正式上交 UWB 类型、QoS 匹配、统一时间域、DDS 发现、丢包/延迟、GCS 10 Hz 稳定性和长时运行。本轮没有跨盒执行，不判定通过。

跨盒验收至少记录 `ros2 topic info --verbose`、`ros2 topic hz`、三车有效位、参考车零位置、yaw 范围、UWB 接受/拒绝计数和 30 分钟无崩溃运行。

### 8.3 RK3588/AArch64

只有在真实 RK3588 Ubuntu 22.04 上完成以下条件才能通过：

- `uname -m` 为 `aarch64`，`file libzju_coop.so` 为 AArch64 ELF；
- 不复制 x86 `.so`，使用目标机原生或经验证交叉工具链重建 SDK；
- 重跑 192 项 C++ 断言、7/7 CTest、14/14 自检和安装树 consumer；
- `ZJU_COOP_SDK_ROOT` 指向 AArch64 staging，重建四个 ROS 2 包并跑 launch 测试；
- 连接真实上交节点和 5G Mesh，量化 CPU、RSS、频率、UWB到Pose延迟和丢包。

本轮只生成 x86-64 ELF，RK3588 门禁未执行。

## 9. 尚缺上交确认的精确资料

1. 正式 UWB 的 ROS 2 包名和完整类型名。字段相同但包名不同的 ROS 消息不能直接 DDS 互通，所以 `zju_coop_test_msgs/UwbRange` 只是当前构建/测试代用类型。
2. `/uwb/range` 和每车 IMU topic 的最终名称、命名空间、发布频率、reliable/best-effort、depth 和 durability。
3. IMU/UWB `header.stamp` 的时间纪元、时钟源和单调性；它必须与融合节点 ROS `now()` 同时域。当前核心允许最多 100 ms 超前、500 ms 接收延迟。
4. IMU 单位、加速度是否为比力、静止水平时 Z 轴符号、`frame_id`、传感器轴到车体 FLU 的外参和协方差可信度。当前节点只校验 frame 名并逐轴复制，不做坐标旋转。
5. UWB `distance` 是三维斜距还是水平距离，标准差/误差模型、无效值规则、NLOS/质量信息，以及三车 IMU 原点到 UWB 天线相位中心的 FLU 杆臂。
6. 车辆 `node_id` 与 UWB `src_id/target_id` 的永久映射；当前测距 C ABI 端点是 `uint16`，节点 ID 参数因此限制在 1..65535。
7. 三车公共 ENU 初始位置、速度、姿态和零偏的来源、生效时刻、精度和 GNSS 锁定到失锁的切换流程。当前只有静态 YAML，没有订阅 GNSS 自动初始化。
8. 重新初始化的触发方、协调停止顺序和整组三个 local＋一个 fusion 的重启顺序。第一版没有会话编号，不支持某一车单独热重新初始化。
9. GCS 实际使用的消息包归属、QoS、最大车辆数和对 `position_valid/yaw_valid=false` 的显示行为。当前字段和 topic 已按发给陆工的版本落盘。
10. 三盒 `ROS_DOMAIN_ID`、Fast DDS discovery 方案、Mesh 网卡和路由/防火墙设置，以及 RK3588 镜像、ROS 安装、部署用户和 systemd/容器方式。
11. 演示时长、车速、最大延迟/丢包、二维位置误差、航向误差、CPU 和内存的验收上限。
12. “SYNC 超帧”是上交无线协议超帧、DW1000 电气 `SYNC` 输入还是二者结合；PPS 是否由硬件定时比较输出，各盒如何获得共同相位、频率和 UTC 秒编号。
13. 模拟 RMC 与 PPS 的配对规则：RMC 在 PPS 前或后发送、对应哪个脉冲、UTC 来源、有效状态、相对 PPS 的 P99 和最大发送延迟。
14. 三盒 PPS 边沿偏差、连续一小时漂移、失去 SYNC/重锁/重启时的时间跳变实测；“百微秒级”必须说明是单盒抖动、盒间偏差、相对超帧还是相对 UTC。
15. 设备时间到 ROS 时间的映射位置和算法。第一版建议验收最终 IMU/UWB `header.stamp` 的跨盒误差 P99 不超过 1 ms、最大不超过 2 ms、连续一小时无时间倒退；核心 100 ms 超前/500 ms 延迟只是输入拒绝保护，不是同步精度指标。

## 10. 主要风险和第一版能力边界

| 项目 | 当前控制 | 明确边界 |
|---|---|---|
| 时间域不一致 | 双时间门限，节流记录 IMU/UWB 拒绝结论 | 上交盒间同步不自动等于 ROS `now()` 使用同一时间纪元 |
| 公共 ENU 初始不正确 | `p/v/q` 必填且缺少时 fail-fast | 没有自动 GNSS 初始化和失锁切换 |
| 单纯 IMU 漂移 | 软件内按初值补偿 `bg/ba`，P15 按随机游走增长 | 零偏不写回硬件，也没有外部观测持续校正它 |
| UWB 时刻错位 | 每车 64 帧缓存，插值，100 ms 有限外推，超限拒绝 | 不做乱序重排或延迟后验回放 |
| 新 NodeState 覆盖 UWB 后验 | 基线 `B` 与持久修正 `C` 分存，输出 `B+C` | NodeState 不当作独立绝对量测反复融合 |
| 无会话编号 | 重新初始化时整组四节点重启，重建 fusion 清空全部历史和后验 | 不支持单车热重启；时间戳不能识别初始化会话 |
| 未反馈 | 参考车持续保存修正 | 不修正从车本地 `p/v/q/bg/ba/P15`，只适用短时理想演示 |
| 姿态 reset Jacobian | 新 UWB 路径不注入本地姿态 | 旧 45 维集中式路径的 reset Jacobian 仍缺失，本轮没有修改 |
| 外参系统误差 | 约定 IMU 已为 FLU，UWB 杆臂默认零 | 这两个前提未在实车确认 |
| 高度和航向可观性 | UWB 用三维斜距、只修正 E/N；yaw 取本地惯导 | 不修正 U/速度/yaw，不融合磁罗盘、GNSS 航向、雷达或视觉 |
| 正式 UWB 类型 | 临时消息严格复制已知四字段 | 包名不同仍不互通；正式接入时只换 ROS 映射层 |
| 带宽 | 跨车只发紧凑 `p/v/q` 状态 | 仍需有一个节点收到三车 NodeState 和 UWB，不是无中心算法 |
| 雷达/视觉扩展 | 保留算法约束入口边界，大数据应在本车前端压缩为小约束 | 当前 ROS 节点不订阅 `PointCloud2`或图像，不声称已融合 |
| Gazebo | 独立可选包、Fortress、ENU/FLU、零杆臂、1 秒静止初始化、平滑起步和 2 号车一次 S 弯、DDS/GZ 双分区隔离；解析仿真继续作快速 CI | headless 同机闭环已通过，但底盘/轮胎/悬架只是首版模型，也不部署到 RK3588 |
| ROS 进程退出 | 2026-08-18 全量回归首次收尾时有一个 local 节点在 SIGINT 后偶发 `-11`；同一用例连续 5 次和随后 12 项全量重跑均通过 | 原因尚未定位；上线守护与长时门禁前必须继续观察退出/重启可靠性，不能用重跑通过抹去该记录 |

`CooperativeCorrection` 反馈的代码接口本轮没有增加。保留的架构边界是：参考车修正器独立于 local handle，未来只能通过新的普通 C 结构和明确的 timestamp/session_id/covariance 契约反馈，不能让 ROS 消息进入核心，也不能把位置修正直接硬覆盖本地 ESKF 名义状态。只有在部署系统能真实生成、持久化并切换 `session_id` 时才应增加该字段，固定常数没有识别能力。

## 11. 持续回归和交付声明

- 每次修改必须先跑目标失败测试，再跑 192 项当前 C++ 断言、7/7 CTest、14/14 自检和两个安装树 consumer。
- ROS 层改动必须重建 staging SDK 和 5 个 ROS/Gazebo 包，再跑非零 UWB 更新测试与必填初值负测试。
- 旧集中式 45 维测试、UWB-only 测试和旧 C ABI 布局断言不得删除或改成宽松断言。
- SDK 和 ROS 产物始终位于工程内 `stage/`、`build/`、`install/`；本次里程碑验收安装到 `stage/zju_coop-milestone-v0.3.0` 和 `install/ros2-milestone-v0.3.0`，没有修改 `/home/pdq/Code/zju_coop`。
- 本里程碑使用中文 Git 提交和 annotated tag 在本地冻结，没有推送远端。
- 本轮只证明 Ubuntu 22.04 x86-64 和同机 ROS 2 Humble/Fast DDS 软件链路；不宣称已完成跨盒 DDS、RK3588、5G Mesh 现场或实车部署。
