# GNSS Initialization and RTK Relative Truth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用标准NavSatFix映射完成WGS84共同ENU初始化和独立RTK相对真值输出。

**Architecture:** 新建无ROS依赖的`GnssReference`核心类与独立C ABI句柄。它先输出既有
二维/15维初始化数组，再持续输出不进入Engine的真值数组。

**Tech Stack:** C++17、普通C ABI、CMake、现有自定义C++测试框架、CTest。

---

### Task 1: GNSS核心类型与WGS84变换

**Files:**
- Create: `src/core/gnss_reference.hpp`
- Create: `src/core/gnss_reference.cpp`
- Create: `tests/test_gnss_reference.cpp`
- Modify: `CMakeLists.txt`

- [ ] 先写经纬高/ECEF/ENU和输入校验失败测试。
- [ ] 构建`zju_coop_tests`，确认因`gnss_reference.hpp`不存在而失败。
- [ ] 实现WGS84椭球变换、协方差旋转、样本缓存和门限检查。
- [ ] 运行核心测试，确认已知位置、速度、不同步和过期行为通过。

### Task 2: GNSS C ABI

**Files:**
- Modify: `include/zju_coop/c_api.h`
- Modify: `src/api/c_api.cpp`
- Modify: `tests/test_c_api.cpp`
- Modify: `tests/c_header_smoke.c`

- [ ] 先写期望的结构初始化、context生命周期、push、初值数组和真值数组测试。
- [ ] 构建测试，确认新增类型/符号缺失导致失败。
- [ ] 声明并实现版本化结构、初始化函数和`zju_coop_gnss_*`公开函数。
- [ ] 验证容量查询、stride、错误码和失败不部分写入。

### Task 3: 文档和SDK示例

**Files:**
- Create: `docs/14_GNSS初始化与RTK相对真值接口说明.md`
- Modify: `docs/04_AIBrainBox部署与上交ROS2适配说明.md`
- Modify: `README.md`
- Modify: `examples/sdk_consumer.cpp`

- [ ] 写出NavSatFix逐字段映射、上海交大RTK topic承诺和调用顺序。
- [ ] 增加最小三节点GNSS初始化/真值示例，不接入Engine更新。
- [ ] 明确杆臂、统一时间、协方差和固定ENU原点要求。

### Task 4: 完整验证

**Files:**
- Verify all modified sources and documentation.

- [ ] 执行完整Release构建。
- [ ] 执行`ctest --test-dir build-vscode -C Release --output-on-failure`。
- [ ] 执行`zju_coop_self_check.exe`。
- [ ] 汇总新增文件、行为边界、命令结果和盒端待确认参数。

由于用户明确禁止未经允许使用Git，本计划不包含commit、push或其他Git命令。

