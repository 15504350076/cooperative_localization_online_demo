# 第二轮高信息量中文注释实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改变可执行行为的前提下，细化全工程高信息量中文注释，并明确无真实数据演示在Windows PowerShell和Ubuntu Bash中的运行方式。

**Architecture:** 注释按“公开契约→数值核心→质量与编排→边界适配→演示工具→测试”的依赖方向补充。每批修改后先做差异审查，最终统一执行Release构建、独立自检、CTest和Python测试，确认没有功能回归。

**Tech Stack:** C++17、C17、CMake、Python 3、Windows PowerShell、Ubuntu 22.04 Bash、Git。

---

### Task 1: 明确使用文档中的终端环境

**Files:**
- Modify: `docs/12_工程使用学习与编写思路.md`
- Reference: `README.md`

- [ ] **Step 1: 将第3节Windows命令标明为PowerShell**

把标题改为“Windows 10/11 PowerShell”，在命令前明确 `.exe`、反斜杠路径和反引号续行均属于PowerShell写法，同时保留三个终端的启动顺序。

- [ ] **Step 2: 增加Ubuntu 22.04 Bash对应命令**

使用以下实际入口，不能把Windows产物写入Linux命令：

```bash
python3 -B tools/gcs_dashboard.py --udp-port 39002 --http-port 8080
./build-arm64/zju_coop_online --config config/demo.ini
python3 -B tools/imu_uwb_simulator.py --host 127.0.0.1 --port 39001
python3 -B tools/imu_online_smoke_test.py \
  --online-exe build-arm64/zju_coop_online \
  --replay-exe build-arm64/zju_coop_replay \
  --config config/demo.ini
```

- [ ] **Step 3: 写明验证边界**

说明两套命令都只验证模拟数据的软件闭环，不证明真实AIBrainBox、ROS 2消息、统一时间轴和实车精度已经验收。

### Task 2: 细化公开类型、C ABI和SDK示例注释

**Files:**
- Modify: `include/zju_coop/c_api.h`
- Modify: `include/zju_coop/export.h`
- Modify: `include/zju_coop/types.hpp`
- Modify: `examples/sdk_consumer.cpp`
- Modify: `src/api/c_api.cpp`

- [ ] **Step 1: 注释公开结构体的数据契约**

为节点号、序号、测量/接收时间、单位、ENU/FLU坐标、协方差、有效位、状态枚举补充语义；对 `struct_size`、`abi_version` 和 `stride` 解释其兼容目的。

- [ ] **Step 2: 注释C ABI生命周期和内存规则**

在创建、惯性配置、输入、两阶段 `step`、销毁接口附近说明调用顺序、调用方缓冲区所有权、同一handle串行调用以及失败不产生部分输出。

- [ ] **Step 3: 注释边界校验阶段**

在 `src/api/c_api.cpp` 标出头部/数组跨度校验、C到C++语义转换、候选输出暂存、异常到错误码转换等高风险阶段，不解释普通字段复制。

- [ ] **Step 4: 让SDK示例体现真实调用意图**

在 `examples/sdk_consumer.cpp` 按“初始化结构→创建会话→配置惯性→推送数据→查询容量→取得快照→销毁”添加阶段说明。

### Task 3: 细化数学与惯性滤波核心注释

**Files:**
- Modify: `src/core/dense_matrix.hpp`
- Modify: `src/core/dense_matrix.cpp`
- Modify: `src/core/quaternion.hpp`
- Modify: `src/core/quaternion.cpp`
- Modify: `src/core/inertial_eskf15.hpp`
- Modify: `src/core/inertial_eskf15.cpp`
- Modify: `src/core/cooperative_inertial_ekf.hpp`
- Modify: `src/core/cooperative_inertial_ekf.cpp`
- Modify: `src/core/range_ekf.hpp`
- Modify: `src/core/range_ekf.cpp`

- [ ] **Step 1: 说明基础数学约定**

解释矩阵维度失败、有限值检查、对称化与Cholesky用途；解释四元数排列、主动旋转、右乘小角度误差和归一化边界。

- [ ] **Step 2: 说明单节点15维ESKF状态与传播**

明确误差排列 `[δp,δv,δθ,δbg,δba]`、ENU/FLU、标准ROS 2 IMU瞬时量、中值积分、重力补偿、首帧只建立时间基准、`Phi/Qd`生成和失败回滚。

- [ ] **Step 3: 说明15N协同测距更新**

在节点块索引、距离预测、两端位置雅可比、NIS、完整联合增益、Joseph协方差、误差注入和相对主参考协方差处注明公式含义及维度。

- [ ] **Step 4: 说明仅测距回退模型边界**

明确二维恒速状态只用于缺少IMU时兼容回退，不能与默认15维惯性模式或绝对定位能力混淆。

### Task 4: 细化质量、拓扑和引擎编排注释

**Files:**
- Modify: `src/core/degradation_monitor.hpp`
- Modify: `src/core/degradation_monitor.cpp`
- Modify: `src/core/rigidity.hpp`
- Modify: `src/core/rigidity.cpp`
- Modify: `src/core/engine.hpp`
- Modify: `src/core/engine.cpp`

- [ ] **Step 1: 注释质量状态机**

说明滑动窗口保存什么、NLOS/有效率/频率/残差如何形成策略，以及正常、降权、暂缓、剔除、试探恢复的进入和退出含义。

- [ ] **Step 2: 注释动态拓扑与可观性**

说明无向边规范化、主参考可达性、二维刚度矩阵的平移/旋转自由度和秩判据；避免把连通误写成完全可观。

- [ ] **Step 3: 注释Engine数据生命周期**

按配置验证、输入时间/重复检查、质量决策、滤波更新、活动边刷新、快照生成和状态判定标注关键阶段，并说明 `step` 不负责通信与车辆控制。

### Task 5: 细化配置、协议、日志和网络边界注释

**Files:**
- Modify: `src/config/ini_config.hpp`
- Modify: `src/config/ini_config.cpp`
- Modify: `config/demo.ini`
- Modify: `config/range_only_demo.ini`
- Modify: `src/protocol/crc32.hpp`
- Modify: `src/protocol/crc32.cpp`
- Modify: `src/protocol/wire_protocol.hpp`
- Modify: `src/protocol/wire_protocol.cpp`
- Modify: `src/protocol/event_log.hpp`
- Modify: `src/protocol/event_log.cpp`
- Modify: `src/net/udp_socket.hpp`
- Modify: `src/net/udp_socket.cpp`

- [ ] **Step 1: 注释配置解析与组合约束**

说明白名单拒绝拼写错误、严格数值解析、单位、默认惯性模式、显式仅测距回退，以及多参数必须共同满足的约束。

- [ ] **Step 2: 注释ZJCL协议边界**

说明固定小端、IEEE-754假设、头部和载荷长度、CRC覆盖范围、有限值/枚举/保留字段验证顺序以及Python黄金字节兼容。

- [ ] **Step 3: 注释ZJLG日志恢复语义**

说明记录类型、长度上限、截断/CRC错误处理，以及回放为何跳过历史输出并重新计算结果。

- [ ] **Step 4: 注释UDP封装的职责边界**

说明跨平台套接字生命周期、超时语义、数据报原子性和最大载荷；明确它是独立演示适配层，不承担无线路由。

### Task 6: 细化在线应用、回放和自检注释

**Files:**
- Modify: `src/apps/app_support.hpp`
- Modify: `src/apps/app_support.cpp`
- Modify: `src/apps/online_main.cpp`
- Modify: `src/apps/replay_main.cpp`
- Modify: `src/apps/self_check_main.cpp`
- Modify: `src/self_check/self_check.hpp`
- Modify: `src/self_check/self_check.cpp`

- [ ] **Step 1: 注释共享会话适配**

说明INI到C ABI映射、会话异常安全、两阶段输出、状态/告警派生及在线与回放复用同一语义的原因。

- [ ] **Step 2: 注释在线进程主循环**

标明启动参数、套接字、输入解码、事件日志、算法推进、遥测发送、统计和安全退出的顺序；说明错误数据被丢弃但进程继续的条件。

- [ ] **Step 3: 注释回放调度**

说明 `speed=0`、按日志时间等待、输入/输出记录区别、stream/final/none模式和回放完成后的最终快照。

- [ ] **Step 4: 注释独立自检的验收范围**

解释确定性静止IMU和3-4-5三边测距构造、每个PASS项证明什么，以及它为何不影响在线进程或实车配置。

### Task 7: 细化Python演示和协议工具注释

**Files:**
- Modify: `tools/zjcl_protocol.py`
- Modify: `tools/imu_uwb_simulator.py`
- Modify: `tools/uwb_simulator.py`
- Modify: `tools/gcs_dashboard.py`
- Modify: `tools/imu_online_smoke_test.py`
- Modify: `tools/online_smoke_test.py`

- [ ] **Step 1: 注释Python协议实现**

在帧布局、CRC、载荷大小、结构化编解码和严格失败条件处说明其必须与C++实现一致，避免解释普通 `struct.pack` 调用。

- [ ] **Step 2: 注释模拟器物理模型**

说明三车3-4-5几何、静止比力、IMU/UWB频率、噪声/NLOS/丢包的随机种子及序号策略。

- [ ] **Step 3: 注释GCS并发和显示语义**

说明UDP接收线程、共享状态加锁、HTTP快照、过期判断和 `yaw_valid/z_valid` 的显示限制。

- [ ] **Step 4: 注释进程级验收判据**

说明子进程启动/清理、临时配置、正常网络证据、超时后告警、恢复和日志回放的通过条件。

### Task 8: 细化测试意图与构建配置注释

**Files:**
- Modify: `tests/c_header_smoke.c`
- Modify: `tests/core_tests.cpp`
- Modify: `tests/test_app_telemetry.cpp`
- Modify: `tests/test_c_api.cpp`
- Modify: `tests/test_cooperative_inertial_ekf.cpp`
- Modify: `tests/test_degradation.cpp`
- Modify: `tests/test_engine.cpp`
- Modify: `tests/test_event_log.cpp`
- Modify: `tests/test_inertial_eskf15.cpp`
- Modify: `tests/test_ini_config.cpp`
- Modify: `tests/test_main.cpp`
- Modify: `tests/test_protocol.cpp`
- Modify: `tests/test_quaternion.cpp`
- Modify: `tests/test_range_ekf.cpp`
- Modify: `tests/test_rigidity.cpp`
- Modify: `tests/test_self_check.cpp`
- Modify: `tests/test_types.cpp`
- Modify: `tests/test_udp_socket.cpp`
- Modify: `tests/test_support.hpp`
- Modify: `tools/test_gcs_dashboard.py`
- Modify: `tools/test_imu_uwb_simulator.py`
- Modify: `tools/test_online_smoke_test.py`
- Modify: `tools/test_uwb_simulator.py`
- Modify: `tools/test_zjcl_protocol.py`
- Modify: `CMakeLists.txt`
- Modify: `cmake/check_elf_exports.cmake`

- [ ] **Step 1: 为测试组添加风险说明**

每个测试文件按场景解释要防止的回归；对黄金字节、数值回滚、重复包、时间边界、ABI容量、线程清理等非直观断言添加原因说明。

- [ ] **Step 2: 注释构建目标和平台分支**

说明动态库、演示程序、自检、安装RPATH、Windows DLL复制、ELF导出检查和CTest进程测试的目的，不解释普通 `target_link_libraries` 语句。

### Task 9: 全量验证、审查并中文提交

**Files:**
- Verify: all modified files

- [ ] **Step 1: 检查本轮差异只包含注释和文档**

Run:

```powershell
git diff --check
git diff --word-diff=porcelain
```

Expected: 无空白错误；若出现非注释代码变化，逐项确认并撤回本轮误改。

- [ ] **Step 2: 重新配置并Release构建**

Run:

```powershell
cmake -S . -B build-vscode -G "Visual Studio 17 2022" -A x64 -DBUILD_TESTING=ON
cmake --build build-vscode --config Release
```

Expected: 两条命令退出码均为0。

- [ ] **Step 3: 运行独立自检和CTest**

Run:

```powershell
.\build-vscode\Release\zju_coop_self_check.exe
ctest --test-dir build-vscode -C Release --output-on-failure
```

Expected: `SELF_CHECK PASS`；CTest全部通过。

- [ ] **Step 4: 运行Python测试**

Run:

```powershell
python -B -m unittest discover -s tools -p 'test_*.py' -v
```

Expected: 全部测试通过且失败数为0。

- [ ] **Step 5: 中文提交且不推送**

Run:

```powershell
git add -A
git diff --cached --check
git commit -m "协同定位演示系统：细化关键模块中文注释和终端说明"
git status --short
```

Expected: 提交成功，工作区干净，不执行 `git push`。
