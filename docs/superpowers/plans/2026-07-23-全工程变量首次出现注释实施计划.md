# 全工程变量首次出现注释实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为全部手写程序中的参数、成员变量、局部变量和循环变量在各自作用域首次声明处补充高信息量中文用途说明。

**Architecture:** 不改变工程结构和运行逻辑，按公开契约、算法核心、应用工具、测试构建四组互不重叠的文件补充注释。后台代理只修改分配文件且不执行Git操作，主代理负责应用层与构建文件、交叉审查、完整验证和中文提交。

**Tech Stack:** C++17、C11公开头、Python 3.11、CMake 3.16、Visual Studio 2022、CTest、PowerShell。

---

### Task 1: 建立声明覆盖审计基线

**Files:**
- Inspect: `include/`, `src/`, `examples/`, `tools/`, `tests/`, `cmake/`, `CMakeLists.txt`

- [ ] **Step 1: 枚举全部手写程序文件**

```powershell
rg --files include src examples tools tests cmake | Sort-Object
```

预期：不包含`build-vscode`、IDE缓存或生成文件。

- [ ] **Step 2: 搜索主要变量声明形态**

```powershell
rg -n "\b(auto|bool|char|double|float|int|size_t|string|vector|array|uint|std::|const auto)\b|for\s*\(|foreach\s*\(|\bself\." include src examples tools tests CMakeLists.txt cmake
```

预期：形成参数、成员、局部量、循环变量和Python实例成员的人工审查入口。

### Task 2: 公开接口、配置和协议变量注释

**Files:**
- Modify: `include/zju_coop/c_api.h`, `include/zju_coop/export.h`, `include/zju_coop/types.hpp`
- Modify: `src/api/c_api.cpp`
- Modify: `src/config/ini_config.hpp`, `src/config/ini_config.cpp`
- Modify: `src/protocol/crc32.hpp`, `src/protocol/crc32.cpp`, `src/protocol/event_log.hpp`, `src/protocol/event_log.cpp`, `src/protocol/wire_protocol.hpp`, `src/protocol/wire_protocol.cpp`

- [ ] **Step 1: 注释公开字段和函数参数**

逐一说明节点编号、序号、统一时间/接收时间、坐标系、单位、数组布局、容量/步长、所有权和有效标志。函数参数说明放在首次公开声明，避免实现处重复。

- [ ] **Step 2: 注释配置与协议局部变量**

重点说明文本游标、字段偏移、载荷长度、CRC覆盖分段、记录长度、调用方缓冲容量和错误码转换变量。

- [ ] **Step 3: 检查本组差异**

```powershell
git diff --check -- include src/api src/config src/protocol
```

预期：无空白错误；表达式、常量和控制流不变。

### Task 3: 数学、15维惯性和测距滤波变量注释

**Files:**
- Modify: `src/core/dense_matrix.hpp`, `src/core/dense_matrix.cpp`, `src/core/quaternion.hpp`, `src/core/quaternion.cpp`
- Modify: `src/core/inertial_eskf15.hpp`, `src/core/inertial_eskf15.cpp`
- Modify: `src/core/cooperative_inertial_ekf.hpp`, `src/core/cooperative_inertial_ekf.cpp`
- Modify: `src/core/range_ekf.hpp`, `src/core/range_ekf.cpp`

- [ ] **Step 1: 注释矩阵、四元数和状态索引变量**

说明矩阵维度、行列角色、主元判据、四元数顺序、主动旋转方向以及15维误差状态分块。

- [ ] **Step 2: 注释惯性传播和测距更新变量**

说明瞬时IMU、ENU/FLU、重力补偿、候选状态、状态转移、过程噪声、节点差向量、预测距离、创新、NIS、Kalman增益、Joseph更新和跨节点协方差。

- [ ] **Step 3: 检查本组差异**

```powershell
git diff --check -- src/core/dense_matrix.* src/core/quaternion.* src/core/inertial_eskf15.* src/core/cooperative_inertial_ekf.* src/core/range_ekf.*
```

预期：数学表达式、索引值和状态维数不变。

### Task 4: 引擎、退化拓扑、网络应用和自检变量注释

**Files:**
- Modify: `src/core/degradation_monitor.hpp`, `src/core/degradation_monitor.cpp`, `src/core/rigidity.hpp`, `src/core/rigidity.cpp`, `src/core/engine.hpp`, `src/core/engine.cpp`
- Modify: `src/net/udp_socket.hpp`, `src/net/udp_socket.cpp`
- Modify: `src/apps/app_support.hpp`, `src/apps/app_support.cpp`, `src/apps/online_main.cpp`, `src/apps/replay_main.cpp`, `src/apps/self_check_main.cpp`
- Modify: `src/self_check/self_check.hpp`, `src/self_check/self_check.cpp`, `examples/sdk_consumer.cpp`

- [ ] **Step 1: 注释质量窗口、图结构和引擎状态变量**

说明滑窗计数、质量状态、无向边键、邻接表、可达集合、刚性矩阵、重复包缓存、输出时间和原因位。

- [ ] **Step 2: 注释网络、应用调度、自检和SDK变量**

说明套接字所有权、地址端口、接收缓冲、统一时间/单调时间/墙上时间、发送序号、周期截止点、三车几何、句柄RAII和两阶段快照缓冲。

- [ ] **Step 3: 检查本组差异**

```powershell
git diff --check -- src/core/degradation_monitor.* src/core/rigidity.* src/core/engine.* src/net src/apps src/self_check examples
```

预期：套接字、调度、状态机和自检逻辑不变。

### Task 5: Python工具与测试变量注释

**Files:**
- Modify: `tools/zjcl_protocol.py`, `tools/uwb_simulator.py`, `tools/imu_uwb_simulator.py`, `tools/gcs_dashboard.py`, `tools/online_smoke_test.py`, `tools/imu_online_smoke_test.py`
- Modify: `tools/test_zjcl_protocol.py`, `tools/test_uwb_simulator.py`, `tools/test_imu_uwb_simulator.py`, `tools/test_gcs_dashboard.py`, `tools/test_online_smoke_test.py`

- [ ] **Step 1: 注释参数、实例成员和局部变量**

函数文档字符串集中解释参数；首次`self.*`赋值说明线程所有权、单位和生命周期；局部量说明协议字段、序号、随机噪声、NLOS偏差、临时端口、子进程和验证证据。

- [ ] **Step 2: 运行Python测试**

```powershell
D:\python311\python.exe -B -m unittest discover -s tools -p 'test_*.py' -v
```

预期：27项测试全部通过。

### Task 6: C/C++测试与构建变量注释

**Files:**
- Modify: `tests/c_header_smoke.c`, `tests/core_tests.cpp`, `tests/test_support.hpp`, `tests/test_main.cpp`, `tests/test_*.cpp`
- Modify: `CMakeLists.txt`, `cmake/check_elf_exports.cmake`

- [ ] **Step 1: 注释测试夹具和场景变量**

对输入包、期望状态、容差、计数器、临时路径、缓冲区和循环下标说明其验证目标。

- [ ] **Step 2: 注释CMake变量和迭代参数**

说明源文件集合、目标名称参数、可见性属性、测试目标迭代项、解释器、安装目录和ELF审计输出。

- [ ] **Step 3: 检查全部差异仅为注释或排版**

```powershell
git diff --check
git diff --stat
git diff -U0 -- '*.cpp' '*.hpp' '*.h' '*.c' '*.py' '*.cmake' 'CMakeLists.txt'
```

预期：新增内容为注释/文档字符串，非注释变化仅允许缩进或空行。

### Task 7: 完整构建与验证

**Files:**
- Verify: `build-vscode/`

- [ ] **Step 1: 配置并编译Windows Release**

```powershell
& 'D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' -S . -B build-vscode -G 'Visual Studio 17 2022' -A x64 -DBUILD_TESTING=ON
& 'D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' --build build-vscode --config Release
```

预期：动态库、在线/回放、自检、SDK示例和测试目标全部生成。

- [ ] **Step 2: 运行自检和CTest**

```powershell
.\build-vscode\Release\zju_coop_self_check.exe
& 'D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\ctest.exe' --test-dir build-vscode -C Release --output-on-failure
```

预期：独立自检13项通过；CTest 7项通过。

### Task 8: 最终审查和中文提交

**Files:**
- Review: all modified source files

- [ ] **Step 1: 检查并暂存计划内文件**

```powershell
git status --short
git diff --check
git add -- CMakeLists.txt cmake include src examples tools tests
git diff --cached --check
git diff --cached --stat
```

预期：无构建产物、无关文件或空白错误。

- [ ] **Step 2: 创建中文本地提交**

```powershell
git commit -m '协同定位演示系统：补充全部变量首次出现用途注释'
```

预期：本地提交成功，不执行`git push`。
