# 全工程中文注释与使用学习指南实施计划

> 本计划由当前会话直接执行；用户要求最终使用中文 Git 提交，提交前不拆分中间提交。

**目标：** 为全部程序模块和关键处理阶段补充有信息量的中文注释，形成完整使用、学习和编写指南。

**架构：** 注释不改变模块边界与程序行为。CMake仍是唯一构建事实源，C ABI仍是上交ROS 2调用边界，核心算法继续与ROS 2消息解耦。

**技术栈：** C++17、C11、CMake、Python 3、MSVC/GCC、CTest。

---

### 任务1：公共接口与核心算法

**文件：** `include/zju_coop/*`、`src/core/*`

- [ ] 为每个文件、公共类型和关键接口补充职责、单位、坐标系及状态语义。
- [ ] 标注15维误差状态、IMU中值传播、协方差传播、测距创新更新和误差注入关键阶段。
- [ ] 标注退化状态机、动态拓扑、刚度与仅测距回退路径的关键判断。

### 任务2：接口适配、协议、网络和应用

**文件：** `src/api/*`、`src/protocol/*`、`src/net/*`、`src/config/*`、`src/apps/*`、`src/self_check/*`、`examples/*`

- [ ] 说明C ABI句柄、线程保护、输入转换、输出快照和错误码边界。
- [ ] 说明协议帧、字节序、CRC、日志记录、UDP生命周期和严格配置校验。
- [ ] 标注在线、回放、自检和SDK示例的初始化、输入、计算、输出与收尾阶段。

### 任务3：测试、Python工具、CMake与配置

**文件：** `tests/*`、`tools/*`、`CMakeLists.txt`、`cmake/*`、`config/*`

- [ ] 每个测试和工具文件增加模块说明，并在关键构造、执行和断言阶段补充注释。
- [ ] 说明模拟器、在线冒烟测试、GCS面板和Python协议实现的数据流。
- [ ] 说明构建目标、平台分支、导出检查和默认/回退配置的用途。

### 任务4：使用、学习和设计文档

**文件：** `README.md`、`docs/12_工程使用学习与编写思路.md`

- [ ] 给出Windows、Ubuntu 22.04和RK3588环境、构建、运行、自检及仿真步骤。
- [ ] 给出按数据类型、核心滤波、系统编排、协议适配和部署联调组织的学习路线。
- [ ] 说明编写原则、数据流、扩展接口和实机联调注意事项。

### 任务5：验证与中文Git提交

- [ ] 检查全部目标文件均存在模块级注释，抽查关键算法和关键分支注释。
- [ ] 执行CMake Release构建、独立自检、CTest和Python语法检查。
- [ ] 审查改动范围，使用中文提交信息提交当前完整版本，不推送远程仓库。
